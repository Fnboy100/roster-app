# Base URL of the FastAPI backend's v1 API (see backend/README.md to run it locally)
VITE_API_BASE_URL=https://restaurant-inventory-api-8h0b.onrender.com/api/v1

# WebSocket URL for live push (same host as the API, ws:// instead of http://)
VITE_WS_BASE_URL=ws://restaurant-inventory-api-8h0b.onrender.com/api/v1
